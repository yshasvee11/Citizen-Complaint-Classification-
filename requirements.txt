# InfraLynx NLP Service — shared requirements
# Install: pip install -r requirements.txt

# Core ML
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
protobuf>=3.20.3

# NLP
spacy>=3.7.2

# API
fastapi>=0.104.1
uvicorn>=0.24.0

# Normalization (Karthik's step, but shared env)
contractions>=0.1.73

# Steps 1-2 (Aayush) — uncomment when his code is ready
# fasttext>=0.9.2
# ai4bharat-transliteration>=1.1.3
