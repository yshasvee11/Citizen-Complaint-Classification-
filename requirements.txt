# InfraLynx NLP Service — unified team requirements
# Install: pip install -r requirements.txt
# Download spaCy model: python -m spacy download en_core_web_sm

# Core ML & Transformers
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
protobuf>=3.20.3
regex>=2023.10.3

# NLP & Text Processing
spacy>=3.7.2
sacremoses>=0.2.0
indic-nlp-library>=0.92

# API Server
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.0.0

# Steps 3-4 Normalization (Karthik)
contractions>=0.1.73

# Steps 1-2 Language Detection & Transliteration (Aayush)
fasttext-wheel>=0.9.2; sys_platform == 'win32'
fasttext>=0.9.2; sys_platform != 'win32'
ai4bharat-transliteration>=1.1.3

